<header class="navbar navbar-fixed-top" role="navigation">
      <div class="container">
        <%= link_to "Jinglr", root_path, class: "logo" %>
        <nav>
          <button class="btn btn-default pull-right">
        <% if user_signed_in? %>
          Signed in as: <%= current_user.email %>
          (<%= link_to "Sign Out", destroy_user_session_path, method: :delete %>)
        <% else %>
          <%= link_to "Sign In", new_user_session_path %> |
          <%= link_to "Sign Up", new_user_registration_path %>
        <% end %>
      </button>
          <ul class="nav navbar-nav pull-right">
          <li><%= link_to "Search List", search_lists_path, class: "navbar-brand" %></li>
          <li><%= link_to "Categories", categories_path, class: "navbar-brand" %></li>
          <li><%= link_to "Wishlists", wishlists_path, class: "navbar-brand" %></li>
          </ul>
        </nav>
      </div>
    </header>